{
  "name": "operation-sentinel-root",
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "start": "bash scripts/web.sh",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}